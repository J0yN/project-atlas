Purpose

Client-side sorting control for project lists. Supports latest, oldest, featured and alphabetical.

Props

- value?: 'latest' | 'oldest' | 'featured' | 'alphabetical'
- onChange?: (v) => void

Accessibility

- Uses native select element for accessibility and keyboard support.

Future

- Map sort options to CMS query parameters on server when integrating.
