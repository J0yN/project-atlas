# favicon

Purpose

Contains favicon files and browser/platform-specific metadata (ico, png, svg, and browserconfig/manifest if needed).

Guidelines

- Include multiple sizes and formats for broad platform coverage (favicon.ico, 32x32.png, 16x16.png, and an SVG fallback).
- Keep a small README documenting how favicons are generated and where references are added in HTML templates.
