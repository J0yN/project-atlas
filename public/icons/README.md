# icons

Purpose

Single-folder source for icon assets used in the app (SVGs and icon sprites).

Conventions

- Store SVG source icons and optionally an exported sprite or optimized PNG fallback.
- Filenames: icon-<name>.svg e.g., icon-search.svg
- Keep icons semantically named and avoid including colors/styles embedded when possible; prefer CSS-controlled fills.
