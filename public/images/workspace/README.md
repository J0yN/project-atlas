# workspace images

Purpose

Screenshots and media that illustrate workspace features, UX flows, and UI components.

Conventions

- Filenames: workspace-<context>-<width>x<height>.<ext>
- Annotate screenshots with a separate metadata file if necessary; do not hardcode text into images that need localization.
