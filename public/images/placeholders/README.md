# placeholders

Purpose

Low-fidelity images used as placeholders, blurred previews, or skeleton visuals while real assets load.

Conventions

- Keep very small file sizes (tiny WebP/AVIF) for fast delivery.
- Use consistent naming like placeholder-<context>-<size>.<ext>
